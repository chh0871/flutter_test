name: interview
description: A basic flutter practical test to use on interviews

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  mock_data: ^2.0.0
  font_awesome_flutter: ^10.3.0
  cached_network_image: ^3.2.3
  provider: ^6.0.5
  fluttertoast: ^8.1.2

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
 